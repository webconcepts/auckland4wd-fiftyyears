{
  "name": "website",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.7.0",
    "react-contenteditable": "^3.2.4",
    "react-dom": "^16.7.0",
    "react-feather": "^1.1.5",
    "react-router-dom": "^4.3.1",
    "react-scripts": "2.1.3"
  },
  "scripts": {
    "start": "npm run tailwind && react-scripts start",
    "build": "npm run tailwind && npm run purgecss && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "tailwind": "tailwind build src/css/main.css -c tailwind.config.js -o src/main.css",
    "purgecss": "purgecss -c purgecss.config.js -o src"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "autoprefixer": "^9.4.4",
    "postcss-cli": "^6.1.1",
    "purgecss": "^1.2.0",
    "tailwindcss": "^0.7.3"
  }
}
